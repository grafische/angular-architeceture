{
  "$schema": "http://json-schema.org/schema",
  "id": "CreateModule",
  "title": "Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the module, first componet, resolver",
      "default": "ng-conf",
      "x-prompt": "Enter the name of the module"
    },
    "path": {
      "type": "string",
      "description": "Specifies the path to create the Order Wizard under",
      "default": ""
    },
    "project": {
      "type": "string",
      "description": "Specifies the project to run the schematic in",
      "default": "defaultProject"
    }
  },
  "required": [],
  "additionalProperties": false
}
