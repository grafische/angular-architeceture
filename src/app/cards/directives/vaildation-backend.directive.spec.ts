import { VaildationBackendDirective } from './vaildation-backend.directive';

describe('VaildationBackendDirective', () => {
  it('should create an instance', () => {
    const directive = new VaildationBackendDirective();
    expect(directive).toBeTruthy();
  });
});
