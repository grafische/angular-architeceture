import { ValidationBackendDirective } from './validation-backend.directive';

describe('ValidationBackendDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationBackendDirective();
    expect(directive).toBeTruthy();
  });
});
