import { CardsEmployeeFilterPipe } from './cards-employee-filter.pipe';

describe('CardsEmployeeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CardsEmployeeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
