import { CardsHoursOrderPipe } from './cards-hours-order.pipe';

describe('CardsHoursOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new CardsHoursOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
