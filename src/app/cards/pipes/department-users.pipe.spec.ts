import { DepartmentUsersPipe } from './department-users.pipe';

describe('DepartmentUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new DepartmentUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
