import { PreloaderCardsResolver } from './preloader-cards.resolver';

describe('Preloader', () => {
  it('should create an instance', () => {
    expect(new PreloaderCardsResolver()).toBeTruthy();
  });
});
