import { PreloaderPhonesResolver } from './preloader-phones.resolver';

describe('Preloader Phones', () => {
  it('should create an instance', () => {
    expect(new PreloaderPhonesResolver()).toBeTruthy();
  });
});
