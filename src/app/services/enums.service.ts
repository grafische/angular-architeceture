import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class EnumsService {

  constructor() { }

  // nameKey<T>(enum: T): T {
  //   Object.keys(enum).find(key => enum[key] === keyStructure)
  // }

  // identity<T>(arg: T): T {
  //   return arg;
  // }
}
