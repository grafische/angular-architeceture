import { FloorMapsDirective } from './floor-maps.directive';

describe('FloorMapsDirective', () => {
  it('should create an instance', () => {
    const directive = new FloorMapsDirective();
    expect(directive).toBeTruthy();
  });
});
