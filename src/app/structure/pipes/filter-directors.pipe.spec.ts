import { FilterDirectorsPipe } from './filter-directors.pipe';

describe('FilterDirectorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDirectorsPipe();
    expect(pipe).toBeTruthy();
  });
});
