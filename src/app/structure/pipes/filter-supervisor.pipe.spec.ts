import { FilterSupervisorPipe } from './filter-supervisor.pipe';

describe('FilterSupervisorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSupervisorPipe();
    expect(pipe).toBeTruthy();
  });
});
