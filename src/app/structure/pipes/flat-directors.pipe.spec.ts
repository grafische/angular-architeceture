import { FlatDirectorsPipe } from './flat-directors.pipe';

describe('FlatDirectorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FlatDirectorsPipe();
    expect(pipe).toBeTruthy();
  });
});
