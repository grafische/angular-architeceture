import { FlatUsersPipe } from './flat-users.pipe';

describe('FlatUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new FlatUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
