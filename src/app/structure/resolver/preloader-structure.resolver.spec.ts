import { PreloaderStructureResolver } from './preloader-structure.resolver';

describe('Preloader Structure', () => {
  it('should create an instance', () => {
    expect(new PreloaderStructureResolver()).toBeTruthy();
  });
});
