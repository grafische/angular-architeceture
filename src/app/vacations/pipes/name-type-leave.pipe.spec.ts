import { NameTypeLeavePipe } from './name-type-leave.pipe';

describe('NameTypeLeavePipe', () => {
  it('create an instance', () => {
    const pipe = new NameTypeLeavePipe();
    expect(pipe).toBeTruthy();
  });
});
